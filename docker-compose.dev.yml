services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${DB_NAME:-allathand}
      POSTGRES_USER: ${DB_USER:-allathand}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-allathand}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
